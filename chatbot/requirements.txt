# fastapi==0.115.0
# uvicorn[standard]==0.30.0
python-multipart==0.0.9
httpx==0.27.0
# PyMuPDF==1.25.5
# Pillow==11.2.1
# pydantic>=2.0


# ──────────────────────────────────────────────
#  Assnani Dental AI Chatbot — Python Dependencies
#  Python 3.10+ recommended (HuggingFace Docker)
# ──────────────────────────────────────────────
 
# Web framework & ASGI server
fastapi==0.115.5
uvicorn[standard]==0.32.0
 
# Data validation
pydantic>=2.0
 
# Async HTTP client (external YOLO / Gemini API calls)
httpx==0.27.2
 
# PDF handling — image extraction & page rasterisation
PyMuPDF>=1.24.13
 
# Image processing
Pillow>=11.0.0
 
# ──────────────────────────────────────────────
#  Notes:
#  - symptom_analyzer, correlation_engine, and dental_expert_system
#    are local modules — no pip install needed for them.
#  - If your local modules use numpy, scikit-learn, etc., add those here.
# ──────────────────────────────────────────────
